---
import { cn } from '@/lib/utils';
import TargetCard from './target-card.astro';
import { targets } from '@/lib/constants';

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<section class={cn('py-16 md:py-24 bg-base-200 text-base-content', className)}>
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <!-- Section Header -->
    <div class="mb-12 text-center">
      <h2 class="mb-4 text-4xl font-extrabold tracking-tight md:text-5xl">
        Dành cho ai?
      </h2>
      <p class="text-base-content/70 mx-auto max-w-2xl text-lg">
        UIT Knowledge phục vụ đa dạng đối tượng từ sinh viên UIT, học viên IT
        đến những người yêu thích công nghệ
      </p>
    </div>

    <!-- Target Cards -->
    <div class="grid grid-cols-1 gap-8 md:grid-cols-3">
      {
        targets.map((target) => (
          <TargetCard
            title={target.title}
            description={target.description}
            icon={target.icon}
            features={target.features}
          />
        ))
      }
    </div>
  </div>
</section>
